import { Button } from "./components/ui/button";

const Home = () => {
  return (
    <div className="flex items-center justify-center w-screen">
      <Button> Hello</Button>
    </div>
  );
};

export default Home;
